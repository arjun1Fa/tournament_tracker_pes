"""Match routes — report, verify, export.

Implemented in Phase 2.
"""
from flask import Blueprint

matches_bp = Blueprint('matches', __name__, url_prefix='/api/matches')

# Phase 2: Match endpoints will be added here.
